# RGo 项目 OpenCode 规则

## Token 使用规则

### 总原则

- 在保证正确性、安全性和完成任务的前提下，始终尽量减少 token 使用；用户要求和必要验证优先于节省 token。
- 先利用已有上下文和项目索引，再读取文件；已知信息不重复搜索、读取、解释或总结。
- 达到足以决策或验证的证据后立即停止扩展调查，避免无目的探索。

### 回复与过程沟通

- 默认简洁：先给结论，只报告关键证据、实际改动、验证结果、风险和必要下一步。
- 不复述用户请求、完整历史、计划原文、未变化内容或工具执行过程。
- 过程更新只在开始工具操作、出现重要发现、需要授权或长任务持续时发送；合并相邻更新。
- 优先使用短段落；仅在比较、映射或步骤确实更清晰时使用列表、表格或图示。
- 错误只摘录最有诊断价值的行；不粘贴完整日志、堆栈、diff 或测试输出。
- 最终回复使用增量摘要：完成了什么、验证了什么、还剩什么；不重复过程更新。

### 搜索、读取与上下文

- 先用 `rg`、`rg --files`、文件清单和符号搜索定位，再读取最小相关行段。
- 为命令设置合理的输出上限，并用路径、模式、行范围、`head`/`tail` 或摘要统计限制输出。
- 不遍历 vendor、构建产物、大型报告或整个仓库，除非任务确实需要；优先读取项目自有代码和相关局部数据。
- 大文件分段读取，只读取命中附近和直接依赖；不要为了“了解全貌”读取全文。
- 优先查看当前任务涉及的文件和已有报告，不重复读取未变文件；使用已有报告时先核对日期和有效性。
- 上下文将溢出时先保留紧凑的事实、决策、文件和验证摘要，丢弃冗长原始输出。

### 工具与命令

- 将互不依赖的只读检查合并或并行执行，减少往返；有依赖或会修改共享状态的操作保持顺序执行。
- 运行程序、构建和测试时必须控制并发，默认串行；任何时刻最多运行一个 CPU 密集型任务，不得并行启动构建、测试、RubySpec 或基准测试。
- 不在后台遗留 CPU 密集型进程；启动新重任务前先确认上一个任务已退出。只读命令可以合并，但不得与重任务并发执行会明显占用 CPU 的扫描、压缩或分析。
- Go 命令默认使用 `GOMAXPROCS=1` 和 `GOFLAGS='-p=1'`（或显式 `-p 1`）；测试默认禁用包内并行，除非任务确实需要验证并发行为。
- 其他工具显式设置 `jobs/workers/threads=1` 或其最低可用值（例如 `make -j1`、测试 runner 单 worker）；未知工具先查帮助中的并发选项，不能确认时按单进程运行。
- 对非交互式长任务，在系统支持时使用较低调度优先级（如 `nice -n 10`）；程序若仍忽略并发限制，再使用项目环境可用的 CPU 配额或单核绑定，并保留超时，避免失控运行。
- 优先使用项目的安全测试包装器，并设置 CPU、内存和超时限制；长任务按小批次运行，每批结束后再决定是否继续。
- 全量 RubySpec 必须按文件或小批次串行运行；先跑直接相关的最小 spec，不因追求速度提高并发。
- 若系统已有明显负载、风扇升速、交互卡顿或命令被资源限制终止，立即停止新增重任务，降低批量或等待负载下降。
- 降载顺序固定为：停止新增任务 → worker/线程降到 1 → 缩小测试批次 → 降低优先级或施加 CPU 配额 → 等待负载恢复；不得用增加并发来弥补运行缓慢。
- 只有用户明确要求或并发本身是测试目标时才可提高并发；提高前说明原因，并使用最小必要并发度，完成后恢复串行默认值。
- 一次工具调用只返回当前决策需要的信息；不要运行“也许有用”的命令。
- 工具失败时先根据错误调整一次；不要无变化地重复同一调用。
- 能用项目已有脚本、报告或缓存回答时，不重新生成昂贵结果；声称当前状态前必须确认其未过期。
- 不使用网络、外部文档、图片、浏览器、插件或子代理，除非用户要求、项目规则要求或它们能实质提升结果。

### 计划、实现与验证

- 默认不执行任何 Git 命令或操作，包括 `status`、`diff`、`log`、`add`、`commit`、分支、标签、合并、推送和 PR；只有用户明确要求具体 Git 操作时才执行。
- 不在功能尚未完成、验证尚未结束或仅达到阶段性进度时提交代码。即使代码与测试已经完成，也默认只保留工作区改动，由用户决定何时检查和提交。
- 简单任务直接执行；只有多步骤、长期或高风险任务才创建计划，且计划只保留当前阶段和下一步。
- 优先最小、局部、可验证的改动；避免顺手重构、重复实现、生成无必要文档或加入未请求功能。
- 修改前先确认现有实现和当前任务涉及的文件，复用项目已有函数、测试、脚本、模板和约定。
- 先运行与改动直接相关的最小测试；根据影响范围逐级扩大，避免每个小改动都运行全量测试。
- 测试通过只报告命令和结果摘要；测试失败只保留首个根因及必要上下文。
- 同一事实只保留一个权威来源；报告、TODO、计划和回复之间避免复制大段相同内容。
- 不为追求更短输出而跳过失败披露、边界条件、安全检查、兼容性检查或完成度审计。

## 遇到问题时的处理方式

### 当遇到 bug 或功能不工作需要调试时

1. **先测试当前功能** - 尽量找出哪些功能正常工作，哪些不工作
2. **记录到 TODO.md** - 不要现在立即修复 bug，而是把问题记录到 `TODO.md` 文件中
3. **继续推进其他任务** - 先完成能完成的部分，稍后再回来处理记录的问题

### 原因

- 避免在某个问题上花费过多时间
- 保持开发进度
- 有些问题可能需要更大的重构才能解决

### 示例

如果遇到 "数组方法调用超时" 这样的问题：
1. 先测试其他功能（整数运算、字符串等）
2. 把问题记录到 TODO.md
3. 继续测试或开发其他部分
